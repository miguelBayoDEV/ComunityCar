<form method="post" action="{{ path('vehiculo_delete', {'id': vehiculo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vehiculo.id) }}">
    <button class="btn">Delete</button>
</form>
